{% extends 'base.html.twig' %}

{% block title %}ColisChimere - Nouvelle Commande{% endblock %}

{% block body %}
    <h1>Nouvelle Commande</h1>

    {{ form_errors(commandeForm) }}

    {{ form_start(commandeForm) }}
        {{ form_row(commandeForm.masse) }}
        {{ form_row(commandeForm.largeur) }}
        {{ form_row(commandeForm.longueur) }}
        {{ form_row(commandeForm.hauteur) }}
        {{ form_row(commandeForm.user) }}        
    {{ form_end(commandeForm) }}
{% endblock %}